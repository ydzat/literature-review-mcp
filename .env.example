# SiliconFlow API Key (必需)
# 获取地址: https://cloud.siliconflow.cn/i/TxUlXG3u
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# 存储目录 (可选)
# 默认: ~/.arxiv-mcp/
# 如果希望使用项目目录，可以设置为相对路径或绝对路径
# ARXIV_MCP_STORAGE=./.arxiv-mcp

# 注意：存储目录包含以下子目录：
#   - arxiv-mcp.db (SQLite 数据库)
#   - pdfs/ (PDF 文件)
#   - texts/ (提取的文本)
#   - generated/ (生成的文件)

# 调试模式 (可选)
# 设置为 true 启用详细日志
DEBUG=false